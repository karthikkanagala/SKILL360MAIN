---
alwaysApply: true
---

Frontend: Use React, Tailwind CSS, and Shadcn/ui. Backend: FastAPI.

The Frontend is in /frontend and Backend is in /backend.

Always use Axios for API calls to http://localhost:8000.

Ensure CORS is enabled in the backend so the UI can talk to it.

Reference @THEORETICAL_FOUNDATIONS.md for all scoring logic.